/* ================================================== */
/* Gulp
/* ================================================== */
// Gulp
require('./gulp');

// Recognise Staging and Live environments. Staging gets changed files. Live gets a zipped release. This is allows for instant review on the staging and rollback capability on live.