/**
 * site.js
 * Controller JS file.
**/

// jQuery
var $ = jQuery = require('jquery');

;(function($, window, undefined){
	'use strict';

    // Object
    var site = site || {};

	// Export object for use.
	window.site  = site;

}(jQuery, window));